#include "main.h"
/**
 *mul -multiplies of two integers
 *@a: is the first paramiter
 *@b: is the second paramiter
 *Return: multiplication of a,b
*/
int mul(int a, int b)
{ int res = a * b;
return (res);
}
