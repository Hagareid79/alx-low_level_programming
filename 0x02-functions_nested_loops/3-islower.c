#include "main.h"
/**
 * _islower - function to chech
 *@c: checks input f function
 * Return: return 1 if lower else return 0 (Success)
*/
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
