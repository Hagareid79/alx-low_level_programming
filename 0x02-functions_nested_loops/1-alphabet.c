#include "main.h"
/**
 * print alphabet -on the character function
 *          to print from a to z
*/

void print_alphabet(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);

	_putchar('\n');
}
