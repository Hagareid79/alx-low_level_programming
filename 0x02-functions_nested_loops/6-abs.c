#include "main.h"
/**
 *_abs -function comput the absolute of number
 *@n: tacks in integer type for function
 *Return: Always 0 (Success)
*/
int _abs(int n)
{
if (n < 0)
n = (-1) * n;
return (n);
}

