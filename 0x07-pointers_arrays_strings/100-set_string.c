#include "main.h"
/**
 * set_string - set the value of pointer to char
 * @s: is the first parameter, pointer to pointer
 * @to: is the second parameter
 * Return: pointer of  char
*/
void set_string(char **s, char *to)
{
*s = to;
}
