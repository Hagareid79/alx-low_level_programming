#include "main.h"
#include <stdio.h>
/**
 * main - prints the number
 * @argc: is a number
 * @argv: is a number
 * Return: zero
*/
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
